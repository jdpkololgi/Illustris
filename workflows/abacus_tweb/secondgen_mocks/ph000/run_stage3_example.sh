#!/usr/bin/env bash
# Example only — edit module paths, --base_output, --pota, --targDir, --mocknum, etc.
#
#   source /global/common/software/desi/desi_environment.sh main
#   python scripts/upstream_mkCat_SecondGen_amtl.py --help
#
# mkCat has many boolean switches; enable only the steps you need for DA2 mocks.
